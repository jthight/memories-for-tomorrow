#!/bin/bash

if [[ -d "/mnt/c/Users/jthig/.dotfiles/" ]]; then
  echo 'Use "git pull" in /mnt/c/Users/jthig/.dotfiles/ to update files!'
else
  # mkdir ~/_tmp_
  # scp -r john@192.168.1.15:~/src/memories-for-tomorrow/files/ ~/_tmp_/
  # cp ~/_tmp_/files/.bash_aliases /mnt/c/Users/jthig/
  # cp ~/_tmp_/files/.bashrc /mnt/c/Users/jthig/
  # cp ~/_tmp_/files/.gitconfig /mnt/c/Users/jthig/
  # cp ~/_tmp_/files/.tmux.conf /mnt/c/Users/jthig/
  # cp ~/_tmp_/files/.vimrc /mnt/c/Users/jthig/
  # cp ~/_tmp_/files/_vimrc /mnt/c/Users/jthig/
  # rm -r ~/_tmp_
  echo "All dot files updated to latest version."
fi
