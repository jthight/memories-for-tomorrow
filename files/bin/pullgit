#!/bin/bash

cd $HOME/src/ansible/
git pull origin main
cd $HOME/src/memories-for-tomorrow/
git pull origin master
cd $HOME/src/secret_stuff/
git pull origin main
cd $HOME/.dotfiles/
git pull origin main
